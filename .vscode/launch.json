{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Otwórz w Chrome",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}